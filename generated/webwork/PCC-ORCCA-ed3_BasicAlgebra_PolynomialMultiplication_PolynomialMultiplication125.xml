<?xml version='1.0' encoding='utf-8'?>
<webwork-reps version="2" webwork2_major_version="2" webwork2_minor_version="20" xml:id="extracted-PCC-ORCCA-ed3_BasicAlgebra_PolynomialMultiplication_PolynomialMultiplication125" assembly-id="PCC-ORCCA-ed3_BasicAlgebra_PolynomialMultiplication_PolynomialMultiplication125">
  <static seed="2691">
    <statement><p><m>\left({5x+4}\right)\left({x^{2}+5x+4}\right)</m></p>
<p><fillin characters="16" name="AnSwEr0001"/></p>
</statement>
    <answer>
      <p>
        <m>5x^{3}+29x^{2}+40x+16</m>
      </p>
    </answer>
    <solution><p>We multiply each term in the <alert>bi</alert>nomial by each term in the <alert>tri</alert>nomial, and then combine like terms</p>
<p><m>\begin{aligned}
              \left({5x+4}\right)({x^{2}+5x+4})\amp ={5x^{3}+25x^{2}+20x+4x^{2}+20x+16}\\
                                \amp ={5x^{3}+29x^{2}+40x+16}
      \end{aligned}</m></p>
</solution>
  </static>
  <rendering-data problemSource="Palladium_Algebra/Appendices/A_9_5_59.pg" origin="generated" domain="https://webwork.pcc.edu" course-id="orcca" user-id="orcca" passwd="orcca" language="en-US"/>
  <pg><![CDATA[
# WeBWorK problem written by Chris Hughes
# Portland Community College
#
# Template:
# Find the product of the monomial and binomial
#
#    ( a x + b ) * ( x^2 + c x + d ) 
#
#
# a, b, c, d can be non-zero, non-1 on the interval [-5,5].
# 
# Last updated: Carl Yao, 7/03/13
# ENDDESCRIPTION

## DBsubject('Algebra')
## DBchapter('Polynomial and Rational Functions')
## DBsection('Polynomial Functions')
## KEYWORDS('exponent','multiply','FOIL','polynomial','binomial','trinomial')
## DBCCSS('A-APR.1')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## Author('Alex Jordan, Carl Yao, Chris Hughes')
## Institution('PCC')

##############################################

DOCUMENT();

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "PGML.pl",
  "PCCmacros.pl",
  "contextLimitedPolynomial.pl",
  "PGcourse.pl",
);

##############################################

Context("Numeric");
Context()->noreduce('(-x)-y','(-x)+y');
$var = 'x';

# setup the binomial: ($a * x + $b)
$a = (random(1.5,4.5,1)+0.5)*random(-1,1,2);
$b = (random(1.5,4.5,1)+0.5)*random(-1,1,2);
@poly1array = ($a,$b);

# setup the trinomial: x^2 + $c x + $d
$c = (random(1.5,4.5,1)+0.5)*random(-1,1,2);
$d = (random(1.5,4.5,1)+0.5)*random(-1,1,2);
@poly2array = (1,$c,$d);

# reduction check
$poly1 = Formula(PolyString(~~@poly1array))->reduce;
$poly2 = Formula(PolyString(~~@poly2array))->reduce;

# for the solution
@productarray = PolyMult(~~@poly1array,~~@poly2array);
$ans = Formula(PolyString(~~@productarray))->reduce;

# make $poly1 and $poly2 into strings
$poly1 = $poly1->substitute(x=>$var)->reduce;
$poly2 = $poly2->substitute(x=>$var)->reduce;

# for the solution
$soln = Formula(PolyMult(~~@poly1array,~~@poly2array,output=>unsimplified,var=>$var))->reduce->reduce;

# and make the answer into a string
$ans = $ans->substitute(x=>$var);

Context("LimitedPolynomial-Strict");
$ans = Formula($ans)->reduce;


##############################################
TEXT(beginproblem());

BEGIN_PGML

[@ PTX_exercisegroup_introduction('Multiply the polynomials.') @]

[`\left([$poly1]\right)\left([$poly2]\right)`]

[_]{$ans}{16}

END_PGML


BEGIN_PGML_SOLUTION

We multiply each term in the *bi*nomial by each term in the *tri*nomial, and then combine like terms

    [`
      \begin{aligned}
              \left([$poly1]\right)([$poly2])&=[$soln]\\
                                &=[$ans]
      \end{aligned}
    `]
END_PGML_SOLUTION
##############################################

ENDDOCUMENT();
]]></pg>
</webwork-reps>
