<?xml version='1.0' encoding='utf-8'?>
<webwork-reps version="2" webwork2_major_version="2" webwork2_minor_version="20" xml:id="extracted-PCC-ORCCA-ed3_BasicAlgebra_PolynomialMultiplication_PolynomialMultiplicationApplication40" assembly-id="PCC-ORCCA-ed3_BasicAlgebra_PolynomialMultiplication_PolynomialMultiplicationApplication40">
  <static seed="2569">
    <statement><p>A rectangle<rsq/>s base can be modeled by <m>{x+6}</m> meters, and its height can be modeled by <m>{x-8}</m> meters. Use a polynomial to represent the rectangle<rsq/>s area in expanded form.</p>
<p><fillin characters="16" name="AnSwEr0001"/></p>
</statement>
    <answer>
      <p>
        <m>x^{2}-2x-48</m>
      </p>
    </answer>
    <solution><p>A rectangle<rsq/>s area formula is:</p>
<p><m>\displaystyle{ \text{area}=(\text{length})\cdot(\text{width}) }</m></p>
<p>After substitution, we have:</p>
<p><m>\displaystyle{\begin{aligned}
   \text{area} \amp = (\text{length})\cdot(\text{width}) \\
                     \amp = ({x+6})({x-8}) \\
                     \amp = {x\mathopen{}\left(x+6\right)-8\mathopen{}\left(x+6\right)} \\
                     \amp = {x^{2}+6x-8x-48} \\
                     \amp = {x^{2}-2x-48}
\end{aligned}
    }</m></p>
<p>The rectangle<rsq/>s area is <m>{x^{2}-2x-48}</m> square meters.</p>
</solution>
  </static>
  <rendering-data problemSource="Palladium_Algebra/Appendices/A_9_5_73.pg" origin="generated" domain="https://webwork.pcc.edu" course-id="orcca" user-id="orcca" passwd="orcca" language="en-US"/>
  <pg><![CDATA[
# WeBWorK problem written by Carl Yao
# Portland Community College
#
# Polynomial multiplication application problem.
# Calculate a rectangle's area.
# Answer is (x+3)(x+5)=x^2+8x+15
# 
# Last updated: Carl Yao, 11/29/2016
# ENDDESCRIPTION

## DBsubject('Algebra')
## DBchapter('Polynomial and Rational Functions')
## DBsection('Polynomial Functions')
## KEYWORDS('exponent','multiply','polynomial','distribute','monomial')
## DBCCSS('A-APR.1')
## TitleText1('')
## EditionText1('')
## AuthorText1('')
## Section1('')
## Problem1('')
## Author('Alex Jordan, Carl Yao, Chris Hughes')
## Institution('PCC')

##############################################

DOCUMENT();

loadMacros(
  "PGstandard.pl",
  "MathObjects.pl",
  "PGML.pl",
  "contextLimitedPolynomial.pl",
  "PGcourse.pl",
);

##############################################

Context("Numeric");
Context()->noreduce('(-x)-y','(-x)+y');
$var = 'x';
#Context()->variables->are($var=>'Real');

$a = random(2,10,1)*random(-1,1,2);
do {$b = random(2,10,1)*random(-1,1,2);} until ($a!=$b);
$ab = $a*$b;
$aPlusB = $a+$b;

$poly1 = Compute("x+$a")->reduce;
$poly2 = Compute("x+$b")->reduce;
$ans = Compute("x**2+$aPlusB*x+$ab")->reduce;

$step2 = Compute("x*($poly1)+$b*($poly1)")->reduce;
$step3 = Compute("x^2+$a*x+$b*x+$b*$a")->reduce;

Context("LimitedPolynomial-Strict");
#Context()->variables->add($var=>'Real');
$ans = Formula($ans);


##############################################
TEXT(beginproblem());

BEGIN_PGML

A rectangle's base can be modeled by [`[$poly1]`] meters, and its height can be modeled by [`[$poly2]`] meters. Use a polynomial to represent the rectangle's area in expanded form.

[_]{$ans}{16}

END_PGML

##########################################################

BEGIN_PGML_SOLUTION

A rectangle's area formula is:

    [`` \text{area}=(\text{length})\cdot(\text{width}) ``]

After substitution, we have:

    [``
\begin{aligned}
   \text{area} &= (\text{length})\cdot(\text{width}) \\
                     &= ([$poly1])([$poly2]) \\
                     &= [$step2] \\
                     &= [$step3] \\
                     &= [$ans]
\end{aligned}
    ``]

The rectangle's area is [`[$ans]`] square meters.

END_PGML_SOLUTION
##############################################

ENDDOCUMENT();
]]></pg>
</webwork-reps>
